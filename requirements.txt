# SDR EH Kontrol Paneli - Python bağımlılıkları
# Not: Yalnızca kod tarafından gerçekten import edilen paketler listelenir.
# Sürümler, kurulumun farklı ortamlarda çözülebilmesi için alt sınır (>=) olarak
# verilmiştir; tam tekrarlanabilirlik gerekiyorsa bu değerler pip freeze ile sabitlenebilir.

# Sayısal işleme ve DSP
numpy>=1.24
scipy>=1.10

# Gerçek zamanlı ses (spec 5.1.3): demodüle analog telsizi hoparlörden dinleme.
# Kurulu değilse arayüz "ses aygıtı yok" der ve çökmez (özellik devre dışı kalır).
sounddevice>=0.4

# Arayüz (PyQt6-Qt6 ve PyQt6_sip, PyQt6 ile birlikte otomatik gelir)
PyQt6>=6.4
pyqtgraph>=0.13

# ESP32 / servo anten kontrolü için seri port haberleşmesi
pyserial>=3.5

# --- SİSTEM (pip DIŞI) BAĞIMLILIKLARI ---
# Aşağıdakiler pip ile DEĞİL, işletim sistemi paketleriyle kurulur. sdr_core.so bunlara
# derleme ve çalışma zamanında ihtiyaç duyar; venv --system-site-packages ile erişir:
#   sudo apt install libuhd-dev uhd-host \
#                    soapysdr-tools libsoapysdr-dev soapysdr-module-uhd \
#                    python3-soapysdr \
#                    cmake build-essential pybind11-dev
#   sudo uhd_images_downloader   # B200mini firmware/FPGA imajları
# Not: SoapySDR Python binding'i (python3-soapysdr) sistemden gelir; pip paketi değildir.
#
# OPSİYONEL — Sayısal amatör telsiz SES ÇÖZÜMÜ (spec 5.1.3): harici DSD-FME.
# Kurulu değilse 4FSK/C4FM TESPİTİ yine çalışır, yalnızca sesin çözülmesi devre dışı kalır.
#   https://github.com/lwvmobile/dsd-fme  (mbelib gerektirir; PATH'e 'dsd-fme' olarak eklenir)
